#ifndef SCENEINFORMATION_H
#define SCENEINFORMATION_H

#include <string.h>
#include <boost/property_tree/ptree.hpp>
#include "Object.hpp"
#include <vector>

class SceneInformation{

public:
// parse the xml manual annotation file by KTH manual annotation tool
  //string object::importObjectsInformation(string xmlFile);
  SceneInformation();
  void setType(string);
  void addObject(object&);
  

private:
  string sceneType;
  int numberOfObjects;
  vector<Object> objectList;

  vector<Object> landmarkObjectList;
};

#endif
